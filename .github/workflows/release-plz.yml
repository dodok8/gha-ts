# Auto-generated by gaji
# Do not edit manually - Edit workflows/release-plz.ts instead
# Generated at: 2026-02-12T17:45:00Z

concurrency:
  cancel-in-progress: false
  group: release-plz-${{ github.ref }}
jobs:
  release-plz-pr:
    permissions:
      contents: write
      pull-requests: write
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v5
      with:
        fetch-depth: 0
        persist-credentials: false
    - uses: dtolnay/rust-toolchain@stable
    - env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      uses: release-plz/action@v0.5
      with:
        command: release-pr
  release-plz-release:
    permissions:
      contents: write
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v5
      with:
        fetch-depth: 0
        persist-credentials: false
    - uses: dtolnay/rust-toolchain@stable
    - env:
        GITHUB_TOKEN: ${{ secrets.PAT }}
      uses: release-plz/action@v0.5
      with:
        command: release
name: Release-plz
on:
  push:
    branches:
    - main
